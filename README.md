# Hierarchical Person Management App

This is a full-stack MERN project for managing people in a hierarchical structure.  
It allows adding people with optional parents and displays a recursive tree.

---

## Clone the project

```bash
git clone https://github.com/Ali-Repository101/ditnus-machine-task.git
cd ditnus-machine-task

Backend Setup
cd backend
npm install

Create .env file:
PORT=5000
MONGO_URI=mongodb+srv://arshad:ArshadAli@arshad-workspace.tmiirm8.mongodb.net/ditnus-machine-task
JWT_SECRET=w1efef313efe6f46e65dww
JWT_EXPIRES_IN=1d

start the server
 npm run dev
Backend will run on: http://localhost:5000

Frontend Setup
cd frontend
npm install

Start frontend:
npm start
Frontend will run on: http://localhost:3000


